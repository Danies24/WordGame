 // const [after, setafter] = useState([])
    // const wordDelete = (id,wordName)=>{
    //     setwordArray(prevItems=>{
         
    //         return prevItems.filter(word => word.id != id);
    //     });
    //     setafter(()=> {return wordName});

    //   }